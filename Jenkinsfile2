pipeline{
    agent any
    stages{
        stage("build code"){
            steps{
                sh "mvn install"
                sh "printenv"
            }
        }
        stage("test code"){
            steps{
                sh "mvn test"
            }
        }
    }
}
